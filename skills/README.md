# gauge Skills

- `skills/status/SKILL.md`: read usage, paginate results, and keep responses small with field masks.
- `skills/mutations/SKILL.md`: add, refresh, and remove accounts safely with dry-run and raw payloads.
- `skills/headless-auth/SKILL.md`: import Playwright storage state without browser interaction.
- Before account operations on an upgraded installation, run `gauge doctor --format json`; if migration is required, dry-run and then execute `gauge migrate`.
